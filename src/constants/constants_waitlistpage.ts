export const HEADING: string = "Join the Waitlist" as const;
export const SUBHEADING: string =
  "Be the first to know when we launch. Enter your email below." as const;
export const EMAIL = {
  lable: "Email address",
  placeholder: "you@example.com",
} as const;
export const BUTTON_TEXT = "Join the Waitlist" as const;
